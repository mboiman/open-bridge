# REASON: a bare string is the shape an author reaches for first, and it drops
# the date, which is the half that lets the note age.
name: examplebox
scope: user
type: macos
status: online
services:
  - slug: nightly-digest
    type: scheduled
    intentionally_absent: "parked"
