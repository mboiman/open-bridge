# REASON: 2026-13-45 matches the shape rule and is still not a date. This is
# the control that proves `format: date` earns its place beside `pattern`,
# because no regex over four-two-two digits can reject a thirteenth month.
name: examplebox
scope: user
type: macos
status: online
services:
  - slug: nightly-digest
    type: scheduled
    intentionally_absent:
      since: "2026-13-45"
      reason: "Turned off after the June incident."
