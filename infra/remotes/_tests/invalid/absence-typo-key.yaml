# REASON: a misspelled key under a permissive parent is accepted in silence,
# and the entry then carries a reason that no consumer will ever read.
#
# The correctly spelled `reason` is present ON PURPOSE. Without it this file
# would fail on the required-key rule as well and could no longer isolate the
# one rule it exists for. A control that trips two rules proves neither.
name: examplebox
scope: user
type: macos
status: online
services:
  - slug: nightly-digest
    type: scheduled
    intentionally_absent:
      since: 2026-06-04
      reason: "Turned off after the June incident."
      resaon: "Turned off after the June incident."
