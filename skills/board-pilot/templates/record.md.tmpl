{marker}
## board-pilot run record{issue}

Edited in place: one notification, at the first post. Machine-written.
`machine` = an exit code this engine read from the pipe.
`agent`   = a model's words — evidence of what it SAID, not that it is right.

| when | stage | kind | criteria | took | outcome |
|---|---|---|---|---:|---|
{rows}

{summary}

{narration}### Not recorded here, and why
- **Cost:** unmeasured. The engine observes no token count, so this reports none rather than a made-up one.
- **Model:** not reported. Nothing pins one and nothing captures one from the child, so the engine observes none.
- **TDD order:** not proven. Green at the end is not red-before-green, and it is not reconstructible afterwards. See the PR dossier.
